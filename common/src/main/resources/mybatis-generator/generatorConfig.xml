<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>
    <!-- 数据库配置文件 -->
    <properties resource="mybatis-generator/generatorConfig-primary.yml"/>

    <context id="MySQL" targetRuntime="MyBatis3">
        <property name="javaFileEncoding" value="UTF-8"/>
        <property name="autoDelimitKeywords" value="true"/>
        <property name="javaFormatter" value="org.mybatis.generator.api.dom.DefaultJavaFormatter"/>
        <property name="xmlFormatter" value="org.mybatis.generator.api.dom.DefaultXmlFormatter"/>

        <!-- 注释生成规则 -->
        <commentGenerator>
            <property name="suppressAllComments" value="false"/>
            <property name="suppressDate" value="true"/>
        </commentGenerator>

        <!-- 数据库连接 -->
        <jdbcConnection driverClass="${driverClass}" connectionURL="${url}" userId="${user}" password="${password}"/>

        <!-- 生成实体类地址 -->
        <javaModelGenerator targetPackage="${modelPackage}" targetProject="${project}"/>
        <!-- 生成mapxml文件 -->
        <sqlMapGenerator targetPackage="${sqlMapperPackage}" targetProject="${resources}"/>
        <!-- 生成mapxml对应client，也就是接口dao -->
        <javaClientGenerator targetPackage="${mapperPackage}" targetProject="${project}" type="XMLMAPPER"/>


        <!-- 以上部分配置固定，无需改动，此处往下开始项目设置 -->


        <!-- appointment表 -->
        <table tableName="user_info" domainObjectName="UserInfo">
            <property name="useActualColumnNames" value="true"/>
            <generatedKey column="id" sqlStatement="SELECT LAST_INSERT_ID()" identity="true"/>
        </table>


    </context>
</generatorConfiguration>